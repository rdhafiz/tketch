const projectStatus = {
    ACTIVE: 'active',
    ARCHIVE: 'archive',
}

const projectStatusArray = [
    {name: projectStatus.ACTIVE, value: projectStatus.ACTIVE},
    {name: projectStatus.ARCHIVE, value: projectStatus.ARCHIVE},
]

module.exports = {projectStatus, projectStatusArray};